/// Represents the layout size passed to [ResponsiveUiBuilder.child].
enum ResponsiveUiSize {
  /// Small layout
  small,

  /// Medium layout
  medium,

  /// Large layout
  large
}

enum RefinedSize { small, medium, large }
